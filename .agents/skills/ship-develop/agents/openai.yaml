interface:
  display_name: "Ship To Develop"
  short_description: "Push, PR, merge, and clean up"
  default_prompt: "Use $ship-develop to ship the current branch into develop with tests, PR creation, merge, and branch cleanup."
policy:
  allow_implicit_invocation: true

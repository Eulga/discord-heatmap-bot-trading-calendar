interface:
  display_name: "Ship To Develop"
  short_description: "Push, PR, request review, merge"
  default_prompt: "Use $ship-develop to ship the current branch into develop with PR creation, an initial Codex review request without waiting, later fix loops, merge, and branch cleanup."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Ship To Develop"
  short_description: "Push, PR, merge, and clean up"
  default_prompt: "Use $ship-develop to ship the current branch into develop with PR creation, Codex review, fix loops, merge, and branch cleanup."
policy:
  allow_implicit_invocation: true

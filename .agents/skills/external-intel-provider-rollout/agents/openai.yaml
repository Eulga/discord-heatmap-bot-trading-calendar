interface:
  display_name: "Intel Provider Rollout Checklist"
  short_description: "Execute an agreed provider rollout safely"
  default_prompt: "Use $external-intel-provider-rollout only after the rollout scope and success criteria are already agreed, then execute the provider rollout checklist and keep status, tests, and docs aligned."
